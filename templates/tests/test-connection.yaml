apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "flaskhello.fullname" . }}-test-connection"
  labels:
    {{- include "flaskhello.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "flaskhello.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
