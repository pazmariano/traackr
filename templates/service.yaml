apiVersion: v1
kind: Service
metadata:
  name: {{ include "flaskhello.fullname" . }}
  labels:
    {{- include "flaskhello.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "flaskhello.selectorLabels" . | nindent 4 }}
